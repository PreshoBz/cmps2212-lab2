Eric Price
Lab 2
In this lab I practice with DOM manipulation by creating simple html documents. In the first instance I selected html elements using the DOM selection such as querySelector and getElementById with error handling. I also created event handlers using the DOM selection using the addEventListener function. I used various call back functions such as classList.remove, .add and .toggle.  In the end I tested my code recroding it in a Youtube video.
